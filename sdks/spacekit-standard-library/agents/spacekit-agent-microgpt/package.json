{
  "name": "spacekit-agent-microgpt-as",
  "version": "0.1.0",
  "description": "Micro-GPT agent (AssemblyScript) – uses microgpt_forward host primitive, build to WASM like the Rust crate",
  "scripts": {
    "build": "npm run build:release",
    "build:release": "asc --config asconfig.json --target release",
    "build:debug": "asc --config asconfig.json --target debug",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "spacekit-assembly-sdk": "file:../../../spacekit-assembly-sdk"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.0"
  }
}
