# HTTP API — index

The **canonical, exhaustive HTTP route surface** lives in [`src/api/mod.rs`](../../src/api/mod.rs).

This folder holds **focused write-ups**:

| Doc | Covers |
|-----|--------|
| [`sql-query-api.md`](sql-query-api.md) | `POST /query/*` structured JSON queries (aggregate, pagination, DSL). |
| [`../STRUCTURED_QUERY_FEATURES.md`](../STRUCTURED_QUERY_FEATURES.md) | Feature matrix for the structured query DSL. |

The root [`README.md`](../README.md) links guides for repository hosting, MCP, sandbox/tx flows, and security.
